export enum EOnlineStatus {
  online = 'online',
  busy = 'busy',
  offline = 'offline',
}

export enum EGender {
  male = 'male',
  female = 'female',
  other = 'other',
}

export enum ERoomType {
  private = 'private',
  group = 'group',
}
